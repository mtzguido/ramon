name: Makefile CI

on:
  push:
jobs:
  build_all:
    strategy:
      matrix:
        os:
        - debian-latest
        - ubuntu-latest
        compiler:
        - gcc
        - clang

    runs-on: ${{matrix.os}}

    steps:
    - uses: actions/checkout@v3
    - name: Make
      run: make CC=${{matrix.compiler}}
